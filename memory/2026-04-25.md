- Flangie CNC quote app: admin area moved to `/internal-admin`, with Supabase email/password login and allowlist via `ADMIN_EMAILS`.
- Added lead/revenue/invoice tracking for `quotes` and a pre-lead monitoring pipeline (`pre_leads`, cron route, summary email, review dashboard).
- User wants the pre-lead finder to store clean Reddit post permalinks, not search/media URLs.
- Flangie CNC quote app: admin area moved to `/internal-admin`, with Supabase email/password login and allowlist via `ADMIN_EMAILS`.
- Added lead/revenue/invoice tracking for `quotes` and a pre-lead monitoring pipeline (`pre_leads`, cron route, summary email, review dashboard).
- User wants the pre-lead finder to store clean Reddit post permalinks, not search/media URLs.
- User wants pre-lead geographic filtering to strongly prefer UK leads, keep unknown-location leads, and reject clearly outside-UK leads unless `PRELEAD_INCLUDE_OUTSIDE_UK=true`.
- Location signal should be surfaced as `uk`, `unknown`, or `outside_uk`, with debug output showing it and separate outside-UK rejection reporting.
- Do not auto-contact anyone from pre-leads.
