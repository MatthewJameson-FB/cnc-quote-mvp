- Flangie lead system: implemented stricter Reddit/manual prelead qualification plus reply suppression. Thread/comment context is allowed only for reply generation/suppression, never for scoring, pre-AI filtering, or AI classification. Keep admin review required; no auto-posting.
- User preferences/constraints: no Facebook scraping, no Meta API/session automation, no auto-message/auto-comment, no cron changes, no spam. For Reddit context, fetch only top 3 top-level comments, max 200 chars each, no nested replies, and do not send raw comments to OpenAI; only tiny heuristic summaries.
- Lead quality rules now emphasise manufacturable discrete parts, UK or unknown location, and reject generic/internal/electrical/motor faults. Allowed discrete-part exceptions include knob, handle, clip, bracket, casing/housing, cover/lid, dial, trim, latch, hinge, panel, mount.
- Added value-based prioritisation for preleads: `value_tier`, `value_score`, `value_reason`. Admin sorting should prioritise high value tier, then `should_reply`, then newest first.
- Built manual-only Discovery Groups admin feature for UK-first Facebook/Instagram discovery. It stores groups and opens best-effort manual search links; it is explicitly separate from Reddit automation.
- Discovery Groups now includes an authenticated internal redirect route (`/internal-admin/discovery-groups/open`) that logs `discovery_group_events` when a group or quick-search chip is opened, while still doing no scraping/posting.
- Validation completed successfully in workspace: `npm run lint`, `npm run build`, and `PRELEAD_DEBUG=true npm run find:preleads` all passed after latest changes.
- Live DB caveat: Supabase schema-cache lag previously caused missing-column errors for new `pre_leads` fields. Latest migration for value fields and discovery groups may still need to be applied/refreshed in the target Supabase environment for runtime writes/reads.
